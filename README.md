# Accessibility Checker App

A web-based platform that evaluates any public website against Australian accessibility requirements.

## 🚀 Getting Started

### Install dependencies
```bash
npm i
```

### Run in development
```bash
npm run dev
```
App will be available at [http://localhost:3000](http://localhost:3000).


## Environment Setup

### Copy the following into your .env file and replace the placeholder with your personal OpenAI API key:

OPENAI_API_KEY=your-openai-key
OPENAI_MODEL=gpt-4o-mini
CORS_ORIGIN=http://localhost:3000
